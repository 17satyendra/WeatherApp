server:
  port: 8085
spring:
  profiles: local
  datasource:
    url: jdbc:mysql://localhost:3306/weather_loc
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect